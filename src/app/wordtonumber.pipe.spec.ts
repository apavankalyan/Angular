import { WordtonumberPipe } from './wordtonumber.pipe';

describe('WordtonumberPipe', () => {
  it('create an instance', () => {
    const pipe = new WordtonumberPipe();
    expect(pipe).toBeTruthy();
  });
});
